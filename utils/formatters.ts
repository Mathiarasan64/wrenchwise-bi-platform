import { formatCurrency, formatPercent, formatCount, formatDate, formatDateTime, formatRelativeTime, cn } from '@/lib/utils';

export {
  formatCurrency,
  formatPercent,
  formatCount,
  formatDate,
  formatDateTime,
  formatRelativeTime,
  cn,
};

export const formatINR = formatCurrency;
